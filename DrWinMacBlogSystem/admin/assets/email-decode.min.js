// Minimal email-decode placeholder to satisfy legacy saved HTML
// If you need full Cloudflare email decode behavior, replace this file
(function () {
  // No-op: leave any elements as-is
  function decodeEmail() {
    // Intentionally left blank for local dev
  }
  if (
    document.readyState === "complete" ||
    document.readyState === "interactive"
  ) {
    setTimeout(decodeEmail, 1);
  } else {
    document.addEventListener("DOMContentLoaded", decodeEmail);
  }
})();
